title: Desktop Environment Comparison
description: A Jekyll-based desktop environment comparison site
baseurl: ""
url: "https://example.com"

theme: null

collections:
  # nem feltétlen kell, most csak data + indexet használunk

defaults:
  - scope:
      path: ""
    values:
      layout: default
